# Atmos Weather UI

A responsive weather dashboard inspired by the supplied reference image.

## Run
Open `index.html` in a browser.

## Customize
- Replace `assets/weather-bg.png` with your own wallpaper.
- Edit the data arrays in `script.js`.
- Connect a weather API later without changing the UI structure.
